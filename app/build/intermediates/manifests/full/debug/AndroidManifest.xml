<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pmdm.ud6_llevamealli"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="10" />

    <!-- Permite que una aplicación para acceder a la ubicación aproximada derivada de -->
    <!-- las fuentes de ubicación de red, tales como antenas de móviles y Wi-Fi gratuita -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permite que una aplicación para acceder a la ubicación precisa de las fuentes de -->
    <!-- ubicación, como GPS, antenas de móviles y Wi-Fi gratuita -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Permite a las aplicaciones abrir sockets de red -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.pmdm.ud6_llevamealli.UD6_LlevameAlli"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>